# Copier ce fichier en `secrets.yaml` (ignoré par git) et remplir les vraies valeurs.
# Ne jamais committer configs/secrets.yaml.

huggingface:
  hf_token: "TON_TOKEN_ICI"  # requis pour SAM3 (facebook/sam3) via day_night_classifier.py
